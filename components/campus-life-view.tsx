import Image from "next/image";
import { ExternalLink } from "lucide-react";
import { campusLifeSections, residencePhones } from "@/content/campus-life";
import { phoneHref } from "@/content/contacts";

export function CampusLifeView() { return <div className="campus-view">{campusLifeSections.map((section, index) => <section id={section.id} className="campus-section" key={section.id}><header><span>{String(index + 1).padStart(2, "0")}</span><div><h2>{section.titleZh}</h2><p>{section.titleEn}</p></div></header>{section.intro && <p className="section-intro">{section.intro}</p>}<div className="campus-items">{section.items.map((item) => <article key={item.title}><h3>{item.title}</h3><p>{item.text}</p>{item.href && <a href={item.href} target="_blank" rel="noopener noreferrer">查看官方页面 <ExternalLink size={14} /></a>}</article>)}</div>{section.id === "living-notes" && <div className="phone-table"><h3>公寓楼前台联系电话</h3>{residencePhones.map(([building, phone]) => <a href={phoneHref(phone)} key={building}><span>{building} 号楼</span><strong>{phone}</strong></a>)}</div>}{section.images && <div className={`image-gallery ${section.images.length > 1 ? "multi" : ""}`}>{section.images.map((image) => <a href={image.src} target="_blank" rel="noopener noreferrer" key={image.src}><Image src={image.src} alt={image.alt} width={1200} height={1680} sizes="(max-width: 768px) 100vw, 50vw" /><span>点击查看原图</span></a>)}</div>}<small className="source-note">来源：Student Handbook PDF，第 {section.sourcePage} 页</small></section>)}</div>; }
